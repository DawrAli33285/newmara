import BusinessAuthForm from "@/components/BusinessAuthForm";

export const metadata = {
  title: "Register your business — Mara Media",
};

export default function BusinessRegisterPage() {
  return <BusinessAuthForm mode="register" />;
}