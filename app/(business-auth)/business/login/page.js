import BusinessAuthForm from "@/components/BusinessAuthForm";

export const metadata = {
  title: "Business login — Mara Media",
};

export default function BusinessLoginPage() {
  return <BusinessAuthForm mode="login" />;
}